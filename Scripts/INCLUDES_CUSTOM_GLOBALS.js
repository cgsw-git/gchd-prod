showDebug = true; //was false changed for facility creation testing
showMessage = true; //was false  changed for facility creation testing 
debugLevel = 3; //was 3  changed for facility creation testing
if (currentUserID == "ADMIN") {
   showDebug = true;
   showMessage = true;
   debugLevel = 3;
}